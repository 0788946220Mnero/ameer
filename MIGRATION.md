# الانتقال من Supabase إلى MongoDB Atlas و Cloudinary

## ما تغيّر فعلًا

طلب «تغيير قاعدة البيانات» بدا تبديلًا واحدًا، لكن Supabase كانت تقدّم
للمشروع أربعة أشياء لا واحدًا:

| الخدمة | كانت | صارت |
|---|---|---|
| قاعدة البيانات | PostgreSQL + 17 هجرة | MongoDB Atlas + 14 مجموعة |
| الحماية على مستوى الصفّ | سياسات RLS داخل القاعدة | فحوص صريحة في كل مسار |
| المصادقة | Supabase Auth | JWT + bcrypt + تدوير التوكنات |
| تخزين الصور | Supabase Storage | Cloudinary برفع موقّع |
| التحديث الفوري | قنوات Realtime | بثّ SSE |

**أهم نتيجة:** لا يوجد في MongoDB ما يقابل RLS. كل ما كانت تضمنه 40 سياسة
داخل PostgreSQL انتقل إلى كود الباك إند. والواجهة لم تعد تتصل بالقاعدة
إطلاقًا — كانت تقرأ منها مباشرة في 22 موضعًا.

---

## قرارات النمذجة

**الصور والشرائح مضمّنة داخل مستند الإعلان** بدل مجموعتين منفصلتين. تُقرأ
دائمًا مع الإعلان ولا يُستعلم عنها وحدها، فالتضمين يلغي ثلاث عمليات `join`
كانت في نسخة PostgreSQL.

**آخر رسالة مخزّنة مع المحادثة.** البديل تجميع على كل الرسائل عند كل فتح
لقائمة المحادثات — أي قراءة مجموعة تنمو بلا حدّ لعرض سطر واحد.

**فهرس فريد على (الإعلان، المشتري)** يمنع تكرار المحادثات على نفس الإعلان
مهما تكرّر الضغط، وهو ما كان يضمنه قيد `UNIQUE` سابقًا.

**التنظيف صريح.** لا `ON DELETE CASCADE` في MongoDB: حذف إعلان ينظّف
محادثاته ورسائله ومفضّلاته ومشاهداته وصوره على Cloudinary يدويًا.

---

## ثغرتان أُغلقتا بالتصميم لا بمشغّل

**رفع الحظر الذاتي.** في نسخة PostgreSQL كانت سياسة `profiles update own`
تسمح للمستخدم بتعديل صفّه، و`is_banned` ضمن الصفّ — فأي محظور يرفع الحظر
عن نفسه بطلب واحد. احتاج ذلك مشغّلًا خاصًا لسدّه.

هنا `is_banned` و`roles` **غير موجودين في مخطّط التعديل إطلاقًا**. أرسِلهما
في الطلب فيُتجاهلا. الثغرة مستحيلة بالتصميم.

**إلغاء قرار الرفض.** الإعلان المرفوض مقفل على مالكه في المسار نفسه، ولا
يتغيّر إلا من الإدارة.

---

## مشاكل ظهرت أثناء البناء وعولجت

**حدّ المحاولات كان يعاقب الأبرياء.** الربط بعنوان IP وحده يبدو صحيحًا،
لكن المكاتب وشبكات الهواتف في الأردن تشترك في عنوان واحد — فموظّف واحد
يخطئ كلمة مروره يقفل الباب على عشرين زميلًا. فُصل: حدّ ضيّق على البريد
(6 محاولات) وهو ما يوقف التخمين فعلًا، وسقف واسع على العنوان (40).

**طلبات التجديد المتوازية كانت ستطرد المستخدم.** الخادم يعتبر إعادة
استخدام توكن التجديد سرقةً فيُنهي كل الجلسات — وهو سلوك صحيح. لكن صفحة
تُطلق خمسة نداءات متوازية بعد انتهاء التوكن كانت سترسل خمسة طلبات تجديد
بنفس التوكن. عميل الـ API الآن يشارك طلب تجديد واحدًا.

**تجميع `$first` كان يقرأ كل الرسائل.** استُبدل بتخزين آخر رسالة مع
المحادثة.

---

## ما لم يتغيّر

كل مكوّنات العرض، والتصميم، والنصوص العربية، وبنية الصفحات. حقول الـ API
تخرج بنفس أسماء PostgreSQL (`snake_case`) عمدًا، فبقيت مكوّنات العرض كما
هي دون تعديل.

---

## نتائج التحقق

جرت الاختبارات على قاعدة حيّة متوافقة مع MongoDB، لا بفحص الأنواع وحده.

### الباك إند — 47 مسارًا، صفر مفقود

| الاختبار | النتيجة |
|---|---|
| بذر الأقسام والمدن | 13 قسمًا و6 مدن |
| رسالة موحّدة للبريد الخاطئ وكلمة المرور الخاطئة | لا تكشف أي البُرد مسجّلة |
| إرسال `is_banned` و`roles` في تعديل الحساب | تُتجاهل |
| إعلان بلا متجر | مرفوض |
| `min_order = 1` | مرفوض (المنصّة للجملة) |
| صورة بمعرّف لا يملكه المستخدم | مرفوضة |
| هاتف البائع للزائر / للمسجّل | محجوب / ظاهر |
| مالك يحاول نشر إعلان مرفوض | مرفوض + إشعار وصل |
| مدير يحظر نفسه أو ينزع إدارته | مرفوض |
| مستخدم عادي يفتح لوحة الإدارة | 403 |
| تدوير توكن التجديد | يعمل؛ إعادة استخدام القديم تُكتشف وتُنهي كل الجلسات |
| تخمين كلمة مرور حساب واحد | يُحجب عند المحاولة السابعة |
| مستخدم آخر من نفس العنوان | يدخل بلا مشكلة |
| البائع يراسل نفسه | مرفوض |
| فتح المحادثة مرتين | محادثة واحدة |
| طرف ثالث يقرأ محادثة | 403 |
| البثّ الفوري | الرسالة والإشعار وصلا خلال أقل من ثانية |
| المفضّلة (إضافة مكرّرة) | عنصر واحد |
| توقيع Cloudinary | مجلد مثبّت، حدّ 5 م.ب |

### الواجهة — 11 صفحة

```
/  /listings  /auth  /auth/callback  /account  /chats
/admin  /contact  /report  /about  /sitemap.xml     → كلها 200
```

- الأقسام الثلاثة تظهر في الصفحة المُصيَّرة (SSR) قادمة من MongoDB
- خريطة الموقع تُبنى من الـ API وتضم روابط الإعلانات والبائعين
- **صفر أثر لكلمة `supabase`** في الصفحة المُصيَّرة أو في الكود
- فحص الأنواع و ESLint: صفر أخطاء في الطرفين

---

## نقاط تستحق الانتباه

**البحث العربي.** `regex` يكفي حتى عشرات الآلاف من الإعلانات. بعدها ستحتاج
Atlas Search — وهي ميزة مدفوعة.

**نسخة واحدة على Railway.** ناقل الأحداث وحدّ المعدّل كلاهما في الذاكرة.
عند تشغيل أكثر من نسخة: الاستقصاء الاحتياطي في البثّ يغطّي الرسائل (خلال
10 ثوانٍ)، لكن حدّ المعدّل يصبح لكل نسخة على حدة — انقله إلى Redis عندها.

**معرّف المتجر عربي** (`مؤسسة-مروان-للجملة-nx23s`). يعمل، لكن الرابط يظهر
مُرمَّزًا عند نسخه. قابل للتحويل إلى حروف لاتينية إن أردت.

**النسخ الاحتياطي.** الطبقة المجانية M0 في Atlas بلا نسخ احتياطي تلقائي.
عند وجود بيانات حقيقية، الترقية إلى M10 تفعّل النسخ المستمر.
